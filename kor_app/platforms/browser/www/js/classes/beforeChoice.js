class BeforeChoice {
    constructor(json, characters_to_display, choices) {
        this.json = json;
        this.characters_to_display = characters_to_display;
        this.choices = choices;
    }
}