class kinematic {
    constructor(json, characters_to_display, assign_quests, place_id, before_choice, display) {
        this.json = json;
        this.characters_to_display = characters_to_display;
        this.assign_quests = assign_quests;
        this.place_id = place_id;
        this.before_choice = before_choice;
        this.display = display;
    }
}